require 'spec_helper'

describe Omniauth::Getdata::Io do
  it 'has a version number' do
    expect(Omniauth::Getdata::Io::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
